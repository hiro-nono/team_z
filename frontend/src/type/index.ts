// csrf_token
export type CsrfTokenType = {
  csrf_token :string,
}

// account
export type AccountType = {
  id :string,
  auth_id :string,
  role :string,
  account_status :string,
  created_at :string,
  updated_at :string,
}